import React, { Component } from 'react';

class Signin extends Component {
    render() {
        return (
            <div>
                sign in from
            </div>
        );
    }
}

export default Signin;
